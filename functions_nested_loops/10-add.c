#include "main.h"

/**
 * add - int
 * @a: the character checked
 * @b: the character checked
 * Description: adds two integers
 * Return: result
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
